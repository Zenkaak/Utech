kind = "web"
previewPath = "/"
title = "UTECH SERVER UNLOCK"
version = "1.0.0"
id = "artifacts/utech-dashboard"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20090

[services.development]
run = "pnpm --filter @workspace/utech-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/utech-dashboard", "run", "build" ]
serve = "static"
publicDir = "artifacts/utech-dashboard/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20090"
BASE_PATH = "/"
